name: Build
on:
  workflow_call:
    inputs:
      project-path:
        required: true
        type: string
        description: 'Path to the solution filter or project to build'
      project-name:
        required: true
        type: string
        description: 'Human-readable name for artifact naming'
      run-tests:
        required: false
        type: boolean
        default: true
      pack:
        required: false
        type: boolean
        default: false

jobs:
  build:
    strategy:
      matrix:
        os: [macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          global-json-file: global.json

      - name: Install workloads
        run: dotnet workload install maui macos maui-tizen

      - name: Install Android SDK dependencies (Windows)
        if: matrix.os == 'windows-latest'
        run: dotnet build -t:InstallAndroidDependencies -f net10.0-android -p:AndroidSdkDirectory="%ANDROID_HOME%" -p:AcceptAndroidSDKLicenses=true src\DevFlow\Microsoft.Maui.DevFlow.Agent\Microsoft.Maui.DevFlow.Agent.csproj
        shell: cmd

      - name: Install Android SDK dependencies (macOS)
        if: matrix.os == 'macos-latest'
        run: dotnet build -t:InstallAndroidDependencies -f net10.0-android -p:AndroidSdkDirectory="$ANDROID_HOME" -p:AcceptAndroidSDKLicenses=true src/DevFlow/Microsoft.Maui.DevFlow.Agent/Microsoft.Maui.DevFlow.Agent.csproj

      - name: Build, Test and Pack (Windows)
        if: matrix.os == 'windows-latest'
        run: >
          eng\common\cibuild.cmd
          -configuration Release
          -prepareMachine
          -projects %CD%\${{ inputs.project-path }}
        shell: cmd

      - name: Build and Test (macOS)
        if: matrix.os == 'macos-latest'
        run: >
          ./eng/common/cibuild.sh
          --configuration Release
          --prepareMachine
          --projects $(pwd)/${{ inputs.project-path }}

      - name: Build Client (Windows)
        if: matrix.os == 'windows-latest'
        run: dotnet build src\Client\Microsoft.Maui.Client\Microsoft.Maui.Client.csproj -c Release
        shell: cmd

      - name: Build Client (macOS)
        if: matrix.os == 'macos-latest'
        run: dotnet build src/Client/Microsoft.Maui.Client/Microsoft.Maui.Client.csproj -c Release

      - name: Test Client (Windows)
        if: matrix.os == 'windows-latest'
        run: dotnet test src\Client\Microsoft.Maui.Client.UnitTests\Microsoft.Maui.Client.UnitTests.csproj -c Release --logger "trx;LogFileName=client-results.trx" --results-directory artifacts\TestResults\Release
        shell: cmd

      - name: Test Client (macOS)
        if: matrix.os == 'macos-latest'
        run: dotnet test src/Client/Microsoft.Maui.Client.UnitTests/Microsoft.Maui.Client.UnitTests.csproj -c Release --logger "trx;LogFileName=client-results.trx" --results-directory artifacts/TestResults/Release

      - name: Upload test results
        if: inputs.run-tests && always()
        uses: actions/upload-artifact@v7
        with:
          name: test-results-${{ inputs.project-name }}-${{ matrix.os }}
          path: artifacts/TestResults/**/*.xml

      - name: Upload packages
        if: inputs.pack && matrix.os == 'windows-latest'
        uses: actions/upload-artifact@v7
        with:
          name: packages-${{ inputs.project-name }}
          path: artifacts/packages/**/*.nupkg
