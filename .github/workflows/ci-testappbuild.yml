name: CI - TestAppBuild

on:
  push:
    branches: [main]
    paths:
      - 'src/TestAppBuild/**'
      - 'eng/**'
      - 'Directory.Build.props'
      - 'Directory.Build.targets'
      - 'Directory.Packages.props'
      - 'global.json'
      - 'NuGet.config'
  pull_request:
    types: [opened, synchronize, reopened, edited]
    branches: [main]
    paths:
      - 'src/TestAppBuild/**'
      - 'eng/**'
      - 'Directory.Build.props'
      - 'Directory.Build.targets'
      - 'Directory.Packages.props'
      - 'global.json'
      - 'NuGet.config'

jobs:
  build:
    uses: ./.github/workflows/_build.yml
    with:
      project-path: src/TestAppBuild/TestAppBuild.slnf
      project-name: testappbuild
      run-tests: true
      pack: true
      install-workloads: false
