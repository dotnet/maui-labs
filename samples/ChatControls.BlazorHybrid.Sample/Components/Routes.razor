@namespace ChatControls.BlazorHybrid.Sample.Components
@using ChatControls.BlazorHybrid.Sample.Components.Pages

@* The sample only ever renders the chat page - the Blazor host is dedicated to it, so we skip
   the router and mount the page directly for a tighter startup path. *@
<Chat />

